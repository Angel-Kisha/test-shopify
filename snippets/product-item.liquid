<div>
  <a href=" {{ product.url }}">
    <img src="{{ product.featured_image | image_url}}" alt="" height="400" width="400">
    <h2>{{ product.title }}</h2>
    <p>{{ product.price | money }}</p>
  </a>
</div>
